# Copy to .env and edit as needed
# Local lab only:
FLASK_SECRET_KEY=change-me-please-32-bytes-min

# If not running only on your own machine:
# - Use a long random secret (at least 32 bytes), not a guessable phrase.
# - Example (Linux/macOS): openssl rand -hex 32
